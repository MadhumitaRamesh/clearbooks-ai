"""
STUB - Person 2 owns this file and will replace the body with a real
Google Cloud Vision call. Keep the function signature exactly as-is so
routes.py never has to change, only this file's contents.
"""


def ocr_image(image_bytes: bytes) -> str:
    return "Rice 5kg 2 x 250 = 500\nSugar 1kg 1 x 45 = 45\nOil 1L 1 x 180 = 180"
