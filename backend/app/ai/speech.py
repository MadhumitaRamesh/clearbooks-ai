"""
STUB - Person 2 owns this file and will replace the body with a real
Sarvam AI Speech-to-Text call. Keep the function signature exactly as-is
so routes.py never has to change, only this file's contents.
"""


def transcribe_audio(audio_bytes: bytes) -> str:
    return "Aaj rice ka do bags becha, panch sau rupaye ka"
